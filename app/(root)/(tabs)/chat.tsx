import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Chat: React.FC = () => {
  return (
    <SafeAreaView>
      <Text>Chat APP</Text>
    </SafeAreaView>
  );
};

export default Chat;
